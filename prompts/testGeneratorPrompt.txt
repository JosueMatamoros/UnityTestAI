You are an expert Software Engineer specialized in testing and quality assurance.
Your output MUST be raw C# code only — no markdown, no explanations, no headings, no code fences, no text before or after the code.

Goal:
Generate the MINIMUM number of Unity Test Framework (NUnit 3.x) PlayMode test methods required to achieve 100% line coverage AND 100% decision coverage for the method `<method-name>` in class `<class-name>`.

LINE COVERAGE: every executable line in the method must be reached by at least one test.
DECISION COVERAGE: every conditional branch (true AND false) must be exercised at least once.
MINIMUM TESTS: design the smallest set of tests that satisfies BOTH metrics simultaneously —
never add a test that does not exercise at least one new uncovered line or branch.

The context below is COMPLETE. All dependencies are already resolved and sliced. Do NOT ask for more class definitions. Generate tests immediately.

INPUT — fully assembled context:
{assembledContext}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{codeAnalysis}MANDATORY STEPS BEFORE WRITING ANY CODE:

━━ STEP 0 — CLASS INSTANTIATION CHECK (mandatory, do this before anything else) ━━

Find the exact declaration of `<class-name>` in the context. It looks like one of:
  (a)  public class <class-name>                       ← no inheritance
  (b)  public class <class-name> : MonoBehaviour       ← Unity component
  (c)  public class <class-name> : SomeOtherClass      ← check if SomeOtherClass extends MonoBehaviour

DECISION:
  Declaration has `: MonoBehaviour` / `: Component` / `: Behaviour` anywhere → AddComponent pattern
  Declaration has NOTHING after the class name, or only non-Unity base classes → new ClassName()

CRITICAL WARNING: A class that uses GameObject as a FIELD TYPE or METHOD PARAMETER is NOT
necessarily a MonoBehaviour. Only `: MonoBehaviour` in the declaration makes it one.
  WRONG assumption: "ShapesArray has GameObject[,] fields → must be MonoBehaviour" ← FALSE
  CORRECT reading:  `public class ShapesArray` (no colon) → plain C# class → new ShapesArray()

Using AddComponent on a plain C# class → CS0311 compile error → tests will not compile.
Using new on a MonoBehaviour → MissingMethodException at runtime → tests will fail.

Write your answer before continuing: "<class-name> declaration: [copy the exact line] → [new / AddComponent]"

━━ STEP 1 — BRANCH ANALYSIS ━━

Read the method body and enumerate every branch:
  1. For each `if` / `else if` / `else`: list the TRUE path and the FALSE path separately
  2. For compound conditions (`A && B`, `A || B`):
     - `A && B` needs THREE test cases: (T,T)=overall TRUE, (F,-)=FALSE via A, (T,F)=FALSE via B
     - `A || B` needs THREE test cases: (T,-)=TRUE via A, (F,T)=TRUE via B, (F,F)=overall FALSE
  3. For each `foreach` / `for` / `while`: add THREE cases:
     a) collection IS EMPTY — loop body never executes
     b) collection has a MATCHING item — loop body executes AND exits early (return/break)
     c) collection is NON-EMPTY but NO item matches — loop exhausts all iterations without returning
     ⚠ Case (c) is required to cover the closing `}` of the loop AND the code that runs after the loop
  4. For each `throw` / `return` inside a loop or condition: verify it has a dedicated test
  5. Write down ALL required input combinations before writing a single test

Only after completing this analysis, write the test methods — one per branch combination.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

REQUIREMENTS:

1. Test class
   - Exactly ONE C# file, exactly ONE [TestFixture] class
   - Class name: Test_<class-name>_<method-name>
   - MANDATORY using statements — ALL must be present in every generated file, no exceptions:
       using NUnit.Framework;
       using UnityEngine;
       using UnityEngine.TestTools;
       using System;
       using System.Collections;
       using System.Collections.Generic;
       using System.Linq;
       using System.Reflection;

2. PlayMode tests — ATTRIBUTE RULES (read carefully)
   - All tests MUST use [UnityTest] and return IEnumerator
   - [UnityTest] takes NO named parameters — it has NO Description property
   - CORRECT:
       [UnityTest]
       [Description("what this test verifies")]
       [Timeout(1000)]
       public IEnumerator SomeTest() { ... }
   - WRONG (WILL NOT COMPILE):
       [UnityTest(Description = "...")] ← UnityTestAttribute has no Description property
   - Each test MUST have [Timeout(1000)] on its own line AFTER [UnityTest]
   - [Description("...")] is from NUnit.Framework — place it as a separate line between [UnityTest] and [Timeout]

3. ONE assertion per test — strictly enforced
   - Each test method contains EXACTLY ONE Assert statement — count it before writing
   - Assert.IsNotNull IS an Assert — using it alongside any other Assert gives 2 asserts = INVALID
   - If you need to verify two things, write two SEPARATE test methods, one Assert each
   - NO branching or loops inside test methods (no if/else, switch, for/foreach/while, try/catch)
   - Branching/loops allowed ONLY in [SetUp], [TearDown], and private helper methods
   - WRONG:
       Assert.IsNotNull(result);
       Assert.IsFalse(((IEnumerable<X>)result).Any()); ← TWO asserts = will be rejected
   - RIGHT:
       Assert.IsFalse(((IEnumerable<X>)result).Any()); ← ONE assert = valid

4. Calling methods via Reflection — GetMethod rules (read carefully)

   ALWAYS use the parameter-types overload of GetMethod:
       public method:  typeof(ClassName).GetMethod("MethodName", new Type[] { typeof(ParamType) })
       private method: typeof(ClassName).GetMethod("MethodName", BindingFlags.NonPublic | BindingFlags.Instance, null, new Type[] { typeof(ParamType) }, null)

   WHY: if the class has ANY other method with the same name (overload), using GetMethod without
   parameter types throws System.Reflection.AmbiguousMatchException at runtime — all tests fail
   in [SetUp] before a single assertion runs.

   WRONG — will throw AmbiguousMatchException when overloads exist:
       typeof(ClassName).GetMethod("MethodName")
       typeof(ClassName).GetMethod("MethodName", BindingFlags.Public | BindingFlags.Instance)
   CORRECT — always safe:
       typeof(ClassName).GetMethod("MethodName", new Type[] { typeof(IEnumerable<GameObject>) })

   - Store the MethodInfo in [SetUp] to avoid repetition
   - CRITICAL — exception wrapping: when a method throws, Reflection.Invoke wraps it inside
     TargetInvocationException. The original exception becomes .InnerException
   - To test that a method throws:
       CORRECT:
         var ex = Assert.Throws<TargetInvocationException>(() => method.Invoke(obj, args));
       WRONG:
         Assert.That(() => method.Invoke(...), Throws.TypeOf<System.Exception>())
         ← Fails because Invoke wraps in TargetInvocationException, not the original type
   - When asserting the return value of method.Invoke():
       CORRECT: Assert.IsNotNull(result);
       WRONG:   Assert.AreEqual(_sut.SomeArray[0], result);
       ← Do NOT compare by array index; use IsNotNull or IsNull to verify presence/absence.

5. Setup and initialization
   - Use the instantiation pattern decided in STEP 0 — do not re-evaluate here
   - Use [SetUp] / [TearDown] for repeated initialization — no duplicated code
   - Instantiate GameObjects at runtime — do NOT use prefabs
   - When using AddComponent (MonoBehaviour only): ALWAYS call go.SetActive(false) first
   - ✗ NEVER call go.SetActive(true) anywhere — triggers Awake/Start → NullReferenceException
   - For empty-collection tests: initialize with empty arrays (new T[]{})
   - For numeric inputs, include both positive and negative values where relevant

6. Coverage completeness checklist — verify before writing code
   □ Every `if` branch: one test where condition is TRUE, one where it is FALSE
   □ Every compound `A && B`: (T,T) true case + (F,-) false via first + (T,F) false via second
   □ Every `foreach`: one test with empty collection, one with matching item, one where loop exhausts without match
   □ Every `throw`: at least one test that reaches that throw
   □ Every `return` inside a loop or condition: at least one test that triggers that return

7. Stubs and helpers
   - Do NOT redefine UnityEngine or third-party engine types (e.g., DG.Tweening)
   - Implement only the minimal stubs necessary to isolate non-engine collaborators
   - All test doubles must be IL2CPP compatible (no dynamic proxies, no Reflection.Emit)

8. Encapsulation
   - Do NOT modify visibility, rename, or alter any existing methods or classes
   - Protected/protected internal methods must never be exposed or wrapped

9. Exception handling
   - Do NOT use try/catch in test methods
   - Every test must pass or fail cleanly — no unhandled exceptions

10. Code quality
    - No TODOs, no placeholders, no partial implementations
    - Code must compile and pass static analysis
    - Proper naming conventions, Clean Code principles

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

MANDATORY ASSERT COUNT — execute before writing any code:
For each test method you are about to write:
  1. Count every line containing "Assert." in that method's body
  2. If count ≥ 2 → STOP — split that method into N separate test methods, one Assert each
  3. Assert.IsNotNull counts as 1 — if you pair it with another Assert you already have 2

This is a hard gate: output with 2+ asserts in any test method will be automatically rejected.

YOU MUST IMPLEMENT ALL TESTS. DO NOT ADD ANY TODO LINE. THE CODE MUST COMPILE AND FULFILL ALL REQUIREMENTS ABOVE.

OUTPUT FORMAT (STRICT):
Return ONLY the final C# code.
- Raw C# only — no markdown fences, no explanations, no headings, no extra text
- Exactly ONE file, exactly ONE [TestFixture] class
- Any non-code text makes the response INVALID
